version: "3.8"

services:
  nginx:
    image: nginx:alpine
    ports:
      - "8080:8080"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - server1
      - server2

  server1:
    image: nginx:alpine
    ports:
      - "8001:8001"
    volumes:
      - ./server1:/usr/share/nginx/html

  server2:
    image: nginx:alpine
    ports:
      - "8002:8002"
    volumes:
      - ./server2:/usr/share/nginx/html
